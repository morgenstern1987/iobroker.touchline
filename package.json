{
  "name": "iobroker.touchline",
  "version": "0.1.0",
  "description": "ioBroker adapter for Roth Touchline (legacy and new API) with optional local bridge webserver",
  "author": "Generated by GPT-5.3-Codex",
  "license": "MIT",
  "main": "main.js",
  "keywords": [
    "ioBroker",
    "touchline",
    "roth",
    "heating"
  ],
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@iobroker/adapter-core": "^3.2.3",
    "axios": "^1.7.9",
    "express": "^4.21.2"
  },
  "scripts": {
    "check": "node --check main.js && node --check lib/touchline-client.js",
    "lint": "eslint .",
    "test": "node --test test/*.test.js",
    "ci": "npm run lint && npm run check && npm test",
    "zip": "zip -r iobroker.touchline.zip . -x '.git/*' -x 'node_modules/*' -x '*.zip'"
  },
  "devDependencies": {
    "eslint": "^9.18.0"
  }
}
